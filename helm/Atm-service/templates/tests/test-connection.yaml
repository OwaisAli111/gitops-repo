apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Atm-service.fullname" . }}-test-connection"
  labels:
    {{- include "Atm-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Atm-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
